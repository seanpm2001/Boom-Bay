#!/usr/local/bin/guile \< ;; I believe this is the proper Shebang
;; Start of script
;; Sample Scheme source code for reference (Wikipedia: Scheme#Minimalism)
;; (define-syntax let
;;  (syntax-rules ()
;;    ((let ((var expr) ...) body ...)
;;      ((lambda (var ...) body ...) expr ...))))
(let (var expr) submarine) body "unlocks at HQ level 7"()
 (let (var expr) divespot1) body ( ()
  (let (var ds1_rewards) gold(1000) wood(900) stone(650) iron(400)))
;; I am going to stop here, I don't know how to work this right now
;; File info
;; File type: Scheme source file (*.ss *.scm)
;; File version: 1 (2022, Tuesday, January 11th at 1:27 pm)
;; Line count (including blank lines and compiler line): 17
;; End of script
